export function App() {
  return (
    <main className="app-shell">
      <section className="intro-card" aria-labelledby="app-title">
        <p className="eyebrow">Supervivencia por turnos</p>
        <h1 id="app-title">The Last Turn</h1>
        <p>La partida se está preparando.</p>
      </section>
    </main>
  );
}
